# ADR 0004: No Web Layer

## Decision

Do not add Django, HTMX, a database, or an admin UI.

## Reason

A static site generator produces flat files and does not require a running web
application.

## Trade-off

There is no browser-based authoring interface.
